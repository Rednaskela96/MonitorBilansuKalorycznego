<UserControl x:Class="MonitorBilansuKalorycznego.View.FoodDatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White" d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <Style x:Key="TabRadioStyle" TargetType="RadioButton">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="Transparent" Padding="0,0,0,5" BorderThickness="0,0,0,2" x:Name="Border">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="#2ECC71"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2ECC71"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock x:Name="TxtHeaderTitle" Text="Twoje produkty" FontSize="28" FontWeight="Bold" Foreground="#222"/>
            <TextBlock x:Name="TxtHeaderSubtitle" Text="Zarządzaj nazwami i wartościami swoich produktów" Foreground="#888" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,25">
            <RadioButton Content="Produkty spożywcze" IsChecked="True" GroupName="DbTabs" 
                         Style="{StaticResource TabRadioStyle}" Checked="Tab_Products_Checked"/>
            <RadioButton Content="Aktywności fizyczne" GroupName="DbTabs" 
                         Style="{StaticResource TabRadioStyle}" Checked="Tab_Activities_Checked"/>
        </StackPanel>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,20" BorderThickness="1" BorderBrush="#EEE">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="1" BorderBrush="#DDD" CornerRadius="4" Height="40">
                    <Grid>
                        <TextBox x:Name="SearchBox" VerticalContentAlignment="Center" Padding="10,0" BorderThickness="0" Background="Transparent"
                                 TextChanged="SearchBox_TextChanged"/>
                        <TextBlock Text="🔍 Wyszukaj..." IsHitTestVisible="False" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#AAA">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <Button Grid.Column="1" Content="+ Dodaj" Click="BtnAdd_Click"
                        Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderThickness="0"
                        Margin="15,0,0,0" Padding="20,0" Height="40" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <DataGrid x:Name="ItemsGrid" Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" 
                  IsReadOnly="True" HeadersVisibility="Column" Background="White" RowHeight="50"
                  BorderThickness="1" BorderBrush="#EEE" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F0F0F0">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F9F9F9"/>
                    <Setter Property="Padding" Value="15,10"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="#444"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#EEE"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="15,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" BorderBrush="Transparent" BorderThickness="0" Background="Transparent">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Background" Value="#F5FFF9"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="2*" FontWeight="SemiBold"/>

                <DataGridTextColumn x:Name="ColKcalProd" Header="Kcal/100g" Binding="{Binding CaloriesPer100g}" Width="100"/>
                <DataGridTextColumn x:Name="ColProt" Header="Białko (g)" Binding="{Binding Protein}" Width="80"/>
                <DataGridTextColumn x:Name="ColCarbs" Header="Węglowodany (g)" Binding="{Binding Carbs}" Width="80"/>
                <DataGridTextColumn x:Name="ColFat" Header="Tłuszcze (g)" Binding="{Binding Fat}" Width="80"/>

                <DataGridTextColumn x:Name="ColKcalAct" Header="Spalanie (kcal/h)" Binding="{Binding CaloriesPerHour}" Width="150" Visibility="Collapsed"/>

                <DataGridTemplateColumn Header="Akcje" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏️" Click="BtnEdit_Click" Background="Transparent" BorderThickness="0" 
                                        Cursor="Hand" ToolTip="Edytuj" Foreground="#2980B9" FontSize="16" Margin="0,0,10,0"/>
                                <Button Content="🗑️" Click="BtnDelete_Click" Background="Transparent" BorderThickness="0" 
                                        Cursor="Hand" ToolTip="Usuń" Foreground="#E74C3C" FontSize="16"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>